# Anthropic API key — used server-side to analyze and tailor resumes.
# Get one at https://console.anthropic.com/settings/keys
# Copy this file to `.env.local` and fill in your key. Never commit real keys.
ANTHROPIC_API_KEY=
