# Run Token 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让后台 service worker 里的生成以登录用户的身份计费 —— 面板把自己可用的 Clerk 身份换成一张 15 分钟的服务端签名令牌，随 `start-run` 交给 worker。

**Architecture:** worker 不再持有 Clerk 客户端（上一版方案已被真实浏览器证伪）。面板调用新的 `POST /api/run-token`，服务端用 `auth()` 取得 userId 并签发一张带 `uid` claim 的短期 JWT；面板把它放进 `start-run` 消息；worker 用它作为该 run 所有 API 调用的 Authorization；服务端 `resolveCaller` 认出 `uid` 并返回 `{kind:"user"}`。

**Tech Stack:** Next.js 15 App Router、`jose`（HS256）、Clerk、Chrome MV3、TypeScript、Vitest

## Global Constraints

- **设计文档：** `docs/superpowers/specs/2026-09-02-run-token-design.md`
- **语言规则：** 代码与注释一律英文。本计划的中文散文不要出现在代码里。
- **TTL 固定 15 分钟。**
- **复用 `DEVICE_TOKEN_SECRET`**，不新增环境变量。
- **匿名路径必须完全不变。** 未登录用户照常用设备令牌运行。登录是增强，不是门槛。
- **绝不静默降级。** 一个存在但无效的 bearer 是 401，不是匿名。已登录但取不到 run token 时，面板拒绝启动，不退回设备身份。
- `extension/manifest.config.ts` 不得改动。
- 服务端测试在仓库根目录运行；扩展测试在 `extension/` 目录运行。
- 单元测试全绿**不构成**验收。验收见计划末尾的浏览器清单。

---

## 文件结构

| 文件 | 状态 | 职责 |
|---|---|---|
| `src/lib/auth.ts` | 修改 | 新增 `issueRunToken` / `verifyBearer`；`verifyDeviceToken` 改为薄封装；`resolveCaller` 认 `uid` |
| `src/lib/__tests__/auth.test.ts` | 修改 | 往返、过期、篡改、**两类令牌互不误认** |
| `src/app/api/run-token/route.ts` | 新建 | Clerk 认证的签发端点 |
| `src/app/api/__tests__/run-token.test.ts` | 新建 | 无会话 → 401 |
| `extension/src/lib/api.ts` | 修改 | `send`/`apiPost` 接受显式身份覆盖 |
| `extension/src/lib/run.ts` | 修改 | `RunOptions.runToken`，透传给三处 `apiPost` |
| `extension/src/lib/runToken.ts` | 新建 | 面板侧取令牌 |
| `extension/src/sidepanel/App.tsx` | 修改 | 发消息前取令牌 |
| `extension/src/background/runs.ts` | 修改 | 透传 `runToken` |
| `extension/src/background/identity.ts` | **删除** | 上一版方案产物 |
| `extension/src/background/index.ts` | 修改 | 移除 Clerk 装载 |

---

## Task 1: 服务端 —— 签发、校验、resolveCaller

**Files:**
- Modify: `src/lib/auth.ts`
- Modify: `src/lib/__tests__/auth.test.ts`

**Interfaces:**
- Consumes: 已有的 `secret()`、`SignJWT`、`jwtVerify`
- Produces:
  - `export async function issueRunToken(userId: string): Promise<string>`
  - `export type BearerIdentity = { kind: "user"; userId: string } | { kind: "device"; deviceId: string }`
  - `export async function verifyBearer(token: string): Promise<BearerIdentity | null>`

- [ ] **Step 1: 写失败的测试**

追加到 `src/lib/__tests__/auth.test.ts`（沿用该文件既有的 `DEVICE_TOKEN_SECRET` 设置方式）：

```ts
describe("run tokens", () => {
  it("round-trips a user id", async () => {
    const token = await issueRunToken("user_abc123");
    expect(await verifyBearer(token)).toEqual({ kind: "user", userId: "user_abc123" });
  });

  it("rejects a tampered token", async () => {
    const token = await issueRunToken("user_abc123");
    const tampered = `${token.slice(0, -4)}AAAA`;
    expect(await verifyBearer(tampered)).toBeNull();
  });

  it("rejects an expired token", async () => {
    // Signed with the same secret and algorithm, but already past its
    // expiry — the one property a short TTL is bought for.
    const past = Math.floor(Date.now() / 1000) - 60;
    const expired = await new SignJWT({ uid: "user_abc123" })
      .setProtectedHeader({ alg: "HS256" })
      .setIssuedAt(past - 60)
      .setExpirationTime(past)
      .sign(new TextEncoder().encode(process.env.DEVICE_TOKEN_SECRET!));
    expect(await verifyBearer(expired)).toBeNull();
  });

  // THE COLLISION TEST. Both token kinds are signed with the SAME secret and
  // the SAME algorithm, so jwtVerify succeeds for either — only the claim
  // tells them apart. If that discrimination is ever lost, a device token
  // would be honoured as a user and every anonymous caller would spend some
  // real account's daily quota.
  it("does not mistake a device token for a run token", async () => {
    const { token } = await issueDeviceToken();
    const identity = await verifyBearer(token);
    expect(identity?.kind).toBe("device");
  });

  it("does not mistake a run token for a device token", async () => {
    const token = await issueRunToken("user_abc123");
    expect(await verifyDeviceToken(token)).toBeNull();
  });
});

describe("resolveCaller with a run token", () => {
  const req = (token: string) =>
    new Request("https://example.com/api/analyze", {
      headers: { Authorization: `Bearer ${token}` },
    });

  it("resolves a run token to the user it was minted for", async () => {
    const token = await issueRunToken("user_abc123");
    const result = await resolveCaller(req(token), null);
    expect(result).toEqual({ ok: true, caller: { kind: "user", userId: "user_abc123" } });
  });

  // The existing rule, restated because this task adds a second way for the
  // bearer branch to succeed: a bearer that is present but invalid is a 401,
  // NEVER a silent downgrade to anon. That refusal is the extension's only
  // way to learn its token died.
  it("still refuses an invalid bearer rather than falling back to anon", async () => {
    const result = await resolveCaller(req("not-a-token"), null);
    expect(result).toEqual({ ok: false, reason: "invalid_token" });
  });
});
```

在该文件顶部的 import 中补上 `issueRunToken`、`verifyBearer`、`SignJWT`（若尚未引入）。

- [ ] **Step 2: 运行测试，确认它失败**

在仓库根目录：`npx vitest run src/lib/__tests__/auth.test.ts`
预期：FAIL，报 `issueRunToken` / `verifyBearer` 未导出

- [ ] **Step 3: 实现**

在 `src/lib/auth.ts` 中，紧接 `DEVICE_TOKEN_TTL` 之后加入：

```ts
/**
 * Short by design. A run token lets the extension's service worker transact
 * as a user, and unlike the Clerk session it was minted from, nothing in the
 * worker can re-mint it — so it has to outlive one generation (about a
 * minute, three API calls) with room for a retry, and not much more. Fifteen
 * minutes bounds a leaked token's damage to that user's remaining daily
 * allowance; it grants nothing else.
 */
const RUN_TOKEN_TTL = "15m";
```

在 `issueDeviceToken` 之后加入：

```ts
/** Mint the token the panel hands its service worker so runs bill to this user. */
export async function issueRunToken(userId: string): Promise<string> {
  return new SignJWT({ uid: userId })
    .setProtectedHeader({ alg: "HS256" })
    .setIssuedAt()
    .setExpirationTime(RUN_TOKEN_TTL)
    .sign(secret());
}

/**
 * What an `Authorization: Bearer` actually is, or null if it is not ours.
 *
 * Both token kinds are signed with the SAME secret and algorithm, so the
 * signature check cannot separate them — the CLAIM does: `uid` for a run
 * token, `did` for a device token. One verify, then discriminate. Two
 * separate verify functions would each accept the other's token at the
 * signature step and rely on a claim check anyway, so this keeps the one
 * decision in one place.
 *
 * `uid` is checked FIRST. A token carrying both claims is not something this
 * server ever mints, and treating such a thing as the weaker identity is the
 * safe direction to be wrong in.
 */
export type BearerIdentity =
  | { kind: "user"; userId: string }
  | { kind: "device"; deviceId: string };

export async function verifyBearer(token: string): Promise<BearerIdentity | null> {
  if (!token) return null;
  try {
    // Pin the algorithm. A Uint8Array key already restricts jose to the HS
    // family, but stating it makes the intent explicit rather than an
    // emergent property of the key type.
    const { payload } = await jwtVerify(token, secret(), { algorithms: ["HS256"] });
    const uid = payload.uid;
    if (typeof uid === "string" && uid.length > 0) return { kind: "user", userId: uid };
    const did = payload.did;
    if (typeof did === "string" && did.length > 0) return { kind: "device", deviceId: did };
    return null;
  } catch {
    return null;
  }
}
```

把既有的 `verifyDeviceToken` 函数体替换为一层薄封装，保留其导出与既有测试：

```ts
/** Returns the device id, or null for anything that is not a valid device token. */
export async function verifyDeviceToken(token: string): Promise<string | null> {
  const identity = await verifyBearer(token);
  return identity?.kind === "device" ? identity.deviceId : null;
}
```

在 `resolveCaller` 中，把 bearer 分支改为：

```ts
  const token = bearer(request);
  if (token !== null) {
    // An Authorization header was sent. It is either good or it is a 401 —
    // never a silent downgrade to anon, or the extension can't learn its
    // token died.
    if (!token) return { ok: false, reason: "invalid_token" };
    const identity = await verifyBearer(token);
    if (!identity) return { ok: false, reason: "invalid_token" };
    // A run token IS the user, minted for the extension's service worker
    // because the worker has no Clerk session of its own to present.
    return identity.kind === "user"
      ? { ok: true, caller: { kind: "user", userId: identity.userId } }
      : { ok: true, caller: { kind: "device", deviceId: identity.deviceId } };
  }
```

- [ ] **Step 4: 运行测试，确认通过**

`npx vitest run src/lib/__tests__/auth.test.ts`
预期：PASS，包含新增测试与既有的全部设备令牌测试

- [ ] **Step 5: 类型检查与 lint**

```bash
npx tsc --noEmit
npm run lint
```

- [ ] **Step 6: 提交**

```bash
git add src/lib/auth.ts src/lib/__tests__/auth.test.ts
git commit -m "feat: mint and verify run tokens alongside device tokens"
```

---

## Task 2: 服务端 —— 签发端点

**Files:**
- Create: `src/app/api/run-token/route.ts`
- Create: `src/app/api/__tests__/run-token.test.ts`

**Interfaces:**
- Consumes: `issueRunToken`（Task 1）、`auth()`（`@clerk/nextjs/server`）、`getIdentity`、`rateLimitResponse`、`DEVICE_TOKEN_RATE_LIMIT`
- Produces: `POST /api/run-token` → `{ token: string }` 或 401

- [ ] **Step 1: 写失败的测试**

创建 `src/app/api/__tests__/run-token.test.ts`。**沿用 `src/app/api/__tests__/charging.test.ts` 的既有写法**：`vi.mock` 必须提升到路由 import 之上，路由用静态 import，KV 用 `resetKV()` 重置，密钥在 `beforeEach` 里设置（与 `src/lib/__tests__/auth.test.ts` 同一个值）。

```ts
import { describe, it, expect, beforeEach, vi } from "vitest";

// Must be hoisted above the route import.
vi.mock("@clerk/nextjs/server", () => ({
  auth: vi.fn(async () => ({ userId: null })),
}));

import { auth } from "@clerk/nextjs/server";
import { resetKV } from "@/lib/kv";
import { verifyBearer } from "@/lib/auth";
import { POST } from "@/app/api/run-token/route";

const IP = "6.6.6.6";

const post = () =>
  new Request("https://example.com/api/run-token", {
    method: "POST",
    headers: { "x-forwarded-for": IP },
  });

beforeEach(() => {
  resetKV();
  vi.mocked(auth).mockResolvedValue({ userId: null } as never);
  process.env.DEVICE_TOKEN_SECRET = "test-secret-value-at-least-32-chars-long";
});

describe("POST /api/run-token", () => {
  // The whole point of the endpoint: with no Clerk session there is no user to
  // mint for, and any answer but 401 would hand an account's quota to an
  // unauthenticated caller.
  it("refuses a caller with no Clerk session", async () => {
    const res = await POST(post());
    expect(res.status).toBe(401);
  });

  it("mints a token that resolves back to the signed-in user", async () => {
    vi.mocked(auth).mockResolvedValue({ userId: "user_abc123" } as never);

    const res = await POST(post());
    expect(res.status).toBe(200);

    const body = (await res.json()) as { token?: string };
    expect(typeof body.token).toBe("string");

    // Checked through the real verifier rather than by decoding the JWT: the
    // property that matters is that the API routes will accept this token as
    // this user, not that it has a particular shape.
    expect(await verifyBearer(body.token!)).toEqual({
      kind: "user",
      userId: "user_abc123",
    });
  });
});
```

> 若 `resetKV` 不是 `@/lib/kv` 的导出名，以该文件的实际导出为准 —— `charging.test.ts` 顶部就是它的用法示例。

- [ ] **Step 2: 运行测试，确认它失败**

`npx vitest run src/app/api/__tests__/run-token.test.ts`
预期：FAIL，无法解析 `../run-token/route`

- [ ] **Step 3: 实现**

创建 `src/app/api/run-token/route.ts`：

```ts
import { NextResponse } from "next/server";
import { auth } from "@clerk/nextjs/server";
import { issueRunToken } from "@/lib/auth";
import { getIdentity } from "@/lib/identity";
import { DEVICE_TOKEN_RATE_LIMIT, rateLimitResponse } from "@/lib/rate-limit";

export const runtime = "nodejs";

/**
 * Exchange a Clerk session for a short-lived token the extension's service
 * worker can run under.
 *
 * Why this exists: the worker is a separate JS realm with no Clerk session of
 * its own, and it cannot be given one — the SDK's storage-backed client can
 * only be seeded from a cookie the panel's client never writes. So the panel,
 * which does have a working Clerk identity, trades it here for something it
 * can pass over chrome.runtime messaging. Without this, every background run
 * transacted as an anonymous device and no user's quota was ever charged.
 *
 * Minting draws on the SAME tight per-IP bucket as device tokens rather than
 * the shared request budget: token acquisition must never compete with real
 * work, and a user mid-session must not be refused a run because they were
 * busy tailoring.
 */
export async function POST(request: Request) {
  const { ip } = getIdentity(request);
  const limited = await rateLimitResponse(ip, DEVICE_TOKEN_RATE_LIMIT);
  if (limited) return limited;

  const { userId } = await auth();
  // No session, no identity to mint. Deliberately NOT a fallback to some
  // weaker caller: this endpoint exists only to answer "who is signed in",
  // and an unauthenticated 200 here would hand an account's quota to anyone.
  if (!userId) {
    return NextResponse.json(
      { error: "Sign in to continue." },
      { status: 401 },
    );
  }

  try {
    return NextResponse.json({ token: await issueRunToken(userId) });
  } catch {
    return NextResponse.json(
      { error: "Run tokens are not configured." },
      { status: 503 },
    );
  }
}
```

- [ ] **Step 4: 运行测试，确认通过**

`npx vitest run src/app/api/__tests__/run-token.test.ts`
预期：PASS，2 个测试

- [ ] **Step 5: 类型检查、lint 与构建**

```bash
npx tsc --noEmit
npm run lint
npm run build
```
构建必须过 —— 新路由要能被 Next.js 正确收录。

- [ ] **Step 6: 提交**

```bash
git add src/app/api/run-token/ src/app/api/__tests__/run-token.test.ts
git commit -m "feat: add POST /api/run-token"
```

---

## Task 3: 扩展 —— 身份覆盖与透传

**Files:**
- Modify: `extension/src/lib/api.ts`
- Modify: `extension/src/lib/run.ts`
- Modify: `extension/src/lib/__tests__/api.test.ts`

**Interfaces:**
- Produces:
  - `apiPost<T>(path, body, overrideToken?: string)`
  - `apiGet<T>(path, overrideToken?: string)`
  - `RunOptions.runToken?: string`

- [ ] **Step 1: 写失败的测试**

追加到 `extension/src/lib/__tests__/api.test.ts`（沿用该文件既有的 fetch stub 与 session mock 方式）：

这些测试沿用该文件已有的模式：每个测试自己 `await setToken(...)`、建一个局部 `fetchMock` 并 `vi.stubGlobal("fetch", fetchMock)`、用顶部的 `json()` 辅助构造响应，再从 `fetchMock.mock.calls[0][1]` 读取 `RequestInit`。**该文件没有共享的 `fetchCalls` 或 `respondWith` 辅助函数 —— 不要引用它们。**

```ts
describe("explicit token override", () => {
  it("sends the override instead of consulting the ambient identity", async () => {
    await setToken("device-token");
    const fetchMock = vi.fn<typeof fetch>(async () => json({}));
    vi.stubGlobal("fetch", fetchMock);

    await apiPost("/api/analyze", { a: 1 }, "run-token-xyz");

    const init = fetchMock.mock.calls[0][1] as RequestInit;
    expect((init.headers as Record<string, string>).Authorization).toBe(
      "Bearer run-token-xyz",
    );
  });

  // THE RULE THIS OVERRIDE EXISTS FOR. A run token is the user's identity in
  // a form the worker cannot re-mint. Refreshing to a device token on 401
  // would succeed, and the run would silently finish against the 3-per-30-days
  // device bucket while the panel showed the user their daily allowance — the
  // exact silent downgrade this whole change is undoing. One fetch, no mint,
  // no retry.
  it("never refreshes to a device token when the override is rejected", async () => {
    await setToken("device-token");
    const fetchMock = vi.fn<typeof fetch>(async () => json({ error: "expired" }, 401));
    vi.stubGlobal("fetch", fetchMock);

    const res = await apiPost("/api/analyze", { a: 1 }, "run-token-xyz");

    expect(res.ok).toBe(false);
    if (!res.ok) expect(res.kind).toBe("session_expired");
    expect(fetchMock).toHaveBeenCalledTimes(1);
  });

  it("falls back to the ambient identity when no override is given", async () => {
    await setToken("device-token");
    const fetchMock = vi.fn<typeof fetch>(async () => json({}));
    vi.stubGlobal("fetch", fetchMock);

    await apiPost("/api/analyze", { a: 1 });

    const init = fetchMock.mock.calls[0][1] as RequestInit;
    expect((init.headers as Record<string, string>).Authorization).toBe(
      "Bearer device-token",
    );
  });
});
```

- [ ] **Step 2: 运行测试，确认它失败**

在 `extension/` 下：`npx vitest run src/lib/__tests__/api.test.ts`
预期：FAIL —— override 被忽略，第一个断言拿到设备令牌

- [ ] **Step 3: 实现 api.ts**

把 `send` 的签名改为：

```ts
async function send<T>(
  path: string,
  init: RequestInit,
  allowRefresh = true,
  overrideToken?: string,
): Promise<ApiResult<T>> {
```

在函数开头，把身份解析替换为：

```ts
  // An explicit token means the caller already settled identity and this
  // request must travel under it. The service worker uses this: it has no
  // Clerk session of its own, so the panel mints a run token and passes it
  // in. `currentAuthToken()` in the worker's realm always answers "device",
  // which is precisely the bug this parameter removes.
  //
  // It is treated as a `clerk` identity, not a `device` one, because that is
  // what it behaves like at the 401 fork below: it carries the user, and
  // nothing on this side can re-mint it.
  const auth: AuthToken | null = overrideToken
    ? { kind: "clerk", token: overrideToken }
    : await currentAuthToken();
```

`AuthToken` 需要从 `./session` 以 type 形式引入。

在最后的 401 刷新分支中把 `overrideToken` 一并传下去，使递归重试保持一致：

```ts
  if (res.status === 401 && allowRefresh) {
    const refreshed = await ensureToken(true);
    if (refreshed) return send<T>(path, init, false, overrideToken);
  }
```

> 注：当 `overrideToken` 存在时，`auth.kind` 为 `"clerk"`，因此上面这个 401 分支不会被触及 —— 它前面那个 `res.status === 401 && auth?.kind === "clerk"` 已经先行返回 `session_expired`。透传只是为了不让签名在递归中悄悄丢参数。

更新三个导出：

```ts
export function apiGet<T>(path: string, overrideToken?: string): Promise<ApiResult<T>> {
  return send<T>(path, { method: "GET" }, true, overrideToken);
}

export function apiPost<T>(
  path: string,
  body: unknown,
  overrideToken?: string,
): Promise<ApiResult<T>> {
  return send<T>(
    path,
    {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    },
    true,
    overrideToken,
  );
}
```

`apiPostForm` 不需要改 —— 简历上传只发生在面板里。

- [ ] **Step 4: 实现 run.ts 的透传**

在 `RunOptions` 中加入：

```ts
  /**
   * The identity this run transacts under, when the caller is the service
   * worker. The worker has no Clerk session of its own; the panel mints this
   * and passes it in. Omitted in the panel, where the ambient identity is
   * already correct.
   */
  runToken?: string;
```

把 `runTailor` 中的三处 `apiPost` 调用都补上第三个参数：

```ts
  const parsed = await apiPost<{ jd: ParsedJD }>(
    "/api/parse-jd",
    { text: jd.text },
    opts.runToken,
  );
```

```ts
  const analyzeCall = apiPost<{ analysis: GapAnalysis; remaining: number | null }>(
    "/api/analyze",
    payload,
    opts.runToken,
  );
  const tailorCall = apiPost<{ tailored: TailorResult; remaining: number | null }>(
    "/api/tailor",
    payload,
    opts.runToken,
  );
```

**三处都要改。** 漏掉 `parse-jd` 会让那一次调用以设备身份发出；漏掉 analyze 或 tailor 会让一次生成的一半计到错误的额度桶上，而界面看不出任何异常。

- [ ] **Step 5: 运行测试，确认通过**

```bash
npx vitest run src/lib/__tests__/api.test.ts src/lib/__tests__/run.test.ts
npx tsc --noEmit
npm run lint
```

- [ ] **Step 6: 提交**

```bash
git add extension/src/lib/api.ts extension/src/lib/run.ts extension/src/lib/__tests__/api.test.ts
git commit -m "feat(extension): let a caller pin the identity a request travels under"
```

---

## Task 4: 扩展 —— 面板取令牌、worker 透传、移除 worker 的 Clerk

**Files:**
- Create: `extension/src/lib/runToken.ts`
- Create: `extension/src/lib/__tests__/runToken.test.ts`
- Modify: `extension/src/sidepanel/App.tsx`
- Modify: `extension/src/sidepanel/__tests__/App.test.tsx`
- Modify: `extension/src/background/runs.ts`
- Modify: `extension/src/background/__tests__/runs.test.ts`
- Modify: `extension/src/background/index.ts`
- Modify: `extension/src/background/__tests__/panel.test.ts`
- Delete: `extension/src/background/identity.ts`
- Delete: `extension/src/background/__tests__/identity.test.ts`
- Delete: `extension/src/background/__tests__/identity-wiring.test.ts`

**Interfaces:**
- Consumes: `apiPost`（Task 3）、`StartRunMessage`
- Produces: `fetchRunToken()`；`StartRunMessage.runToken?: string`

- [ ] **Step 1: 写失败的测试**

创建 `extension/src/lib/__tests__/runToken.test.ts`：

```ts
import { describe, it, expect, vi, beforeEach } from "vitest";

let postResult: unknown = { ok: true, data: { token: "run-token-xyz" } };
let signedIn = true;

vi.mock("@/lib/api", () => ({ apiPost: async () => postResult }));
vi.mock("@/lib/clerk", () => ({ isSignedIn: async () => signedIn }));

beforeEach(() => {
  vi.resetModules();
  postResult = { ok: true, data: { token: "run-token-xyz" } };
  signedIn = true;
});

describe("fetchRunToken", () => {
  it("returns the token for a signed-in user", async () => {
    const { fetchRunToken } = await import("@/lib/runToken");
    expect(await fetchRunToken()).toEqual({ ok: true, token: "run-token-xyz" });
  });

  // Anonymous callers get null, NOT a failure. The run still starts, on the
  // device identity — signing in is an upgrade, never a gate.
  it("returns null when the user is not signed in", async () => {
    signedIn = false;
    const { fetchRunToken } = await import("@/lib/runToken");
    expect(await fetchRunToken()).toBeNull();
  });

  // Signed in but the mint failed. This must NOT collapse into the anonymous
  // case: doing so would spend the user's device trial while the panel showed
  // them their daily allowance.
  it("reports the failure when a signed-in user cannot get a token", async () => {
    postResult = { ok: false, kind: "session_expired", message: "Sign in again." };
    const { fetchRunToken } = await import("@/lib/runToken");
    const result = await fetchRunToken();
    expect(result).toEqual({
      ok: false,
      kind: "session_expired",
      message: "Sign in again.",
    });
  });

  it("reports a malformed response rather than returning an empty token", async () => {
    postResult = { ok: true, data: {} };
    const { fetchRunToken } = await import("@/lib/runToken");
    const result = await fetchRunToken();
    expect(result).toMatchObject({ ok: false });
  });
});
```

追加到 `extension/src/background/__tests__/runs.test.ts`：

```ts
  it("passes the message's run token through to the run", async () => {
    await startRun({ ...msg(), runToken: "run-token-xyz" });
    expect(runTailorOpts.at(-1)?.runToken).toBe("run-token-xyz");
  });

  // No token means an anonymous caller. The run proceeds on the device
  // identity, exactly as it does today.
  it("starts without a run token for an anonymous caller", async () => {
    await startRun(msg());
    expect(runTailorOpts.at(-1)?.runToken).toBeUndefined();
  });
```

`runs.test.ts` 顶部记录 `runTailorOpts` 的类型需要加上 `runToken?: string`。

- [ ] **Step 2: 运行测试，确认它们失败**

```bash
npx vitest run src/lib/__tests__/runToken.test.ts src/background/__tests__/runs.test.ts
```
预期：FAIL —— `@/lib/runToken` 无法解析；`runToken` 未被透传

- [ ] **Step 3: 实现 runToken.ts**

创建 `extension/src/lib/runToken.ts`：

```ts
import { apiPost } from "./api";
import { isSignedIn } from "./clerk";
import type { ApiErrorKind } from "./api";

/**
 * The identity a background run should transact under.
 *
 * Three outcomes, deliberately distinct — collapsing any two of them
 * reintroduces a silent downgrade:
 *
 * - `null`      — not signed in. The run proceeds on the device identity,
 *                 exactly as it always has. Signing in is an upgrade, never
 *                 a gate.
 * - `{ok:true}` — a token the worker can run under.
 * - `{ok:false}`— signed in, but no token could be minted. The caller must
 *                 REFUSE to start rather than fall back to the device
 *                 identity: that fallback would succeed, spend the 3-per-30-
 *                 days trial, and leave the panel showing a signed-in user
 *                 their daily allowance while the run billed elsewhere.
 */
export type RunTokenResult =
  | { ok: true; token: string }
  | { ok: false; kind: ApiErrorKind; message: string };

export async function fetchRunToken(): Promise<RunTokenResult | null> {
  if (!(await isSignedIn())) return null;

  const res = await apiPost<{ token?: unknown }>("/api/run-token", {});
  if (!res.ok) return { ok: false, kind: res.kind, message: res.message };

  const token = res.data.token;
  if (typeof token !== "string" || !token) {
    // A 200 with nothing usable in it. Reported as a failure rather than
    // returned as an empty token, which would travel as `Bearer ` and come
    // back a confusing 401 from the far side of three API calls.
    return {
      ok: false,
      kind: "server",
      message: "Couldn't confirm your session. Try again.",
    };
  }
  return { ok: true, token };
}
```

- [ ] **Step 4: 实现 worker 侧的透传**

在 `extension/src/background/runs.ts` 的 `StartRunMessage` 中加入：

```ts
  /**
   * The identity this run transacts under. Minted by the panel, which has a
   * working Clerk session; the worker has none of its own. Absent for an
   * anonymous caller, who runs on the device identity.
   */
  runToken?: string;
```

在 `startRun` 内 `runTailor` 的调用处，把它加入 opts：

```ts
        { extraInfo: msg.supplement, runId, runToken: msg.runToken },
```

- [ ] **Step 5: 实现面板侧**

在 `extension/src/sidepanel/App.tsx` 中 import：

```ts
import { fetchRunToken } from "@/lib/runToken";
```

在构造 `start-run` 消息之前（`chrome.runtime.sendMessage` 调用之前）插入：

```ts
      // Identity is settled HERE, not in the worker. The panel is the only
      // context with a working Clerk session — the worker's realm cannot be
      // given one — so it mints the token the run will travel under and hands
      // it over with the request.
      const runToken = await fetchRunToken();
      if (runToken && !runToken.ok) {
        // Signed in, but no token. Refuse rather than fall back to the device
        // identity: that would quietly bill the run to the 3-per-30-days
        // trial while the header still showed this user their daily
        // allowance. `null` is different and does NOT land here — that is an
        // anonymous caller, who proceeds normally.
        setNotice(runToken.message);
        setStarting(false);
        return;
      }
```

在消息体中加入该字段：

```ts
        runToken: runToken?.ok ? runToken.token : undefined,
```

> 注意 `setStarting(false)` 后的提前 return 会跳过 `finally` 之外的清理 —— 若该处已被 `try/finally` 包裹（当前实现是），则不要重复调用 `setStarting(false)`，直接 `return` 即可，由 `finally` 负责。实施时以文件的实际结构为准。

- [ ] **Step 6: 移除 worker 的 Clerk 客户端**

```bash
git rm extension/src/background/identity.ts
git rm extension/src/background/__tests__/identity.test.ts
git rm extension/src/background/__tests__/identity-wiring.test.ts
```

在 `extension/src/background/index.ts` 中删除：
- `import { installBackgroundClerkTokenSource } from "./identity";`
- 模块作用域的 `installBackgroundClerkTokenSource();` 调用及其上方注释

在 `extension/src/background/__tests__/panel.test.ts` 中删除 `vi.mock("@clerk/chrome-extension/background", ...)`。`chrome` stub 里的 `storage` 可以保留（无害），删掉也行。

- [ ] **Step 7: 面板测试**

追加到 `extension/src/sidepanel/__tests__/App.test.tsx`（紧接既有的并发上限测试之后，沿用同样的 `sendMessageImpl` / `renderApp` / `findButton` / `flush` 模式）：

```ts
  // Signed in but unable to mint: the run must not start at all. Falling back
  // to the device identity here is the silent downgrade this whole change
  // exists to remove.
  it("refuses to start when a signed-in user cannot get a run token", async () => {
    runTokenResult = { ok: false, kind: "session_expired", message: "Sign in again." };
    activeJdState = { jd: JD_A, failure: null, loading: false };
    await setResume(STORED_RESUME);
    await renderApp();

    await act(async () => {
      findButton(container, "Tailor my resume").click();
    });
    await flush();

    expect(sendMessageCalls).toHaveLength(0);
    expect(container.textContent).toContain("Sign in again.");
  });

  // Anonymous callers are unaffected — the run starts with no token.
  it("starts without a run token when nobody is signed in", async () => {
    runTokenResult = null;
    activeJdState = { jd: JD_A, failure: null, loading: false };
    await setResume(STORED_RESUME);
    await renderApp();

    await act(async () => {
      findButton(container, "Tailor my resume").click();
    });
    await flush();

    expect(sendMessageCalls).toHaveLength(1);
    expect((sendMessageCalls[0] as { runToken?: string }).runToken).toBeUndefined();
  });
```

在该文件顶部加入 mock 与其可变结果，形式与既有的 `sendMessageImpl` 一致：

```ts
let runTokenResult: unknown = { ok: true, token: "run-token-xyz" };
vi.mock("@/lib/runToken", () => ({ fetchRunToken: async () => runTokenResult }));
```

并在 `beforeEach` 中重置 `runTokenResult = { ok: true, token: "run-token-xyz" };`

- [ ] **Step 8: 全量验证**

```bash
npx vitest run
npx vitest run
npm run lint
npm run build
```
两次全量必须计数一致（本分支此前出现过间歇性失败）。`npm run build` 必须过。

- [ ] **Step 9: 提交**

```bash
git add -A extension/src
git commit -m "feat(extension): run under a panel-minted run token, not the worker's device identity"
```

---

## 真实浏览器验证（唯一的验收标准）

单元测试全绿**不构成**通过 —— 上一版有 249 个绿色测试而功能是坏的。

1. `npm run build`，在 `chrome://extensions` 重新加载 `extension/dist`
2. 确认装的是新包：`grep -c "run-token" extension/dist/assets/*.js` 应有非零命中
3. 清掉 beta code：面板 console 里 `await chrome.storage.local.remove('cp_beta_code')` —— **不清的话服务端会跳过额度检查，整个测试无效**
4. 登录，记下面板的 "runs left today"
5. **跑一次生成。该数字必须从 5 变成 4。这是唯一的通过标准。** 不减就是没修好，不要继续往下测
6. 连续跑到用尽，确认服务端返回 402 且面板给出额度用尽提示
7. 登出，再跑一次，确认匿名/设备路径仍然可用
8. 开三个招聘页并行生成，确认后台并发未被破坏
9. 确认 service worker 不再加载 Clerk：`chrome://extensions` 上 worker 无报错，且生成正常

---

## Self-Review 记录

**Spec 覆盖：** 逐条对照 —— `issueRunToken`/`verifyBearer`/TTL 15m/复用密钥（Task 1）、claim 互不误认（Task 1 测试）、`resolveCaller` 认 `uid` 且无效 bearer 仍 401（Task 1）、`/api/run-token` 与 401（Task 2）、`api.ts` 身份覆盖且不刷新（Task 3）、三处 `apiPost` 透传（Task 3）、`fetchRunToken` 三态（Task 4）、面板拒绝启动（Task 4）、匿名不变（Task 3/4 均有断言）、删除 worker 的 Clerk（Task 4）、验收清单（末节）。无遗漏。

**占位符扫描：** 无 TBD/TODO。Task 4 Step 5 对 `setStarting(false)` 的处理留有"以文件实际结构为准"的判断余地 —— 这是有意的，因为该处是否已被 `try/finally` 包裹取决于实施时的代码，且计划已明确指出两种情况各自该怎么做。

**类型一致性：** `BearerIdentity` 在 Task 1 定义、Task 1 与 Task 2 使用；`overrideToken` 参数在 Task 3 定义、Task 4 经 `RunOptions.runToken` 使用；`RunTokenResult` 在 Task 4 定义并在同一 Task 内消费。一致。
